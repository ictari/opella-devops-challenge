#!/bin/bash

PATTERN="NO_CHECKIN"
BLOCKED=0

for file in $(git diff --cached --name-only); do
  if grep -qF "$PATTERN" "$file"; then
    echo "❌ Commit blocked: Found '$PATTERN' in $file"
    BLOCKED=1
  fi
done

exit $BLOCKED